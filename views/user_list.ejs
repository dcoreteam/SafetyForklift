<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>User Management</title>
    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >
  </head>
  <body>
    <!-- Include Partial Navbar -->
    <%- include('partials/navbar') %>
    <div class="container mt-4">
      <h1>User Management</h1>

      <!-- ปุ่มสำหรับเพิ่ม User (ถ้ามีฟังก์ชัน Add) -->
      <div class="mb-3">
        <a href="/management/user/add" class="btn btn-primary">Add User</a>
      </div>

      <!-- ตารางแสดงผู้ใช้ -->
      <table class="table table-bordered table-striped table-hover">
        <thead>
          <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Company Name</th>
            <th>View Map</th>
            <th>Create Staff</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (users.length === 0) { %>
            <tr>
              <td colspan="6" class="text-center">No users found.</td>
            </tr>
          <% } else { %>
            <% users.forEach(user => { %>
              <tr>
                <td><%= user.user_id %></td>
                <td><%= user.username %></td>
                <td><%= user.company_name %></td>
                <!-- view_map และ create_staff เป็น boolean สามารถแสดงเป็น Yes/No หรือ icon -->
                <td><%= user.view_map ? 'Yes' : 'No' %></td>
                <td><%= user.create_staff ? 'Yes' : 'No' %></td>
                <td>
                  <!-- ตัวอย่างปุ่ม Edit/Delete -->
                  <a
                    href="/management/user/edit/<%= user.user_id %>"
                    class="btn btn-sm btn-warning"
                  >
                    Edit
                  </a>
                  <form
                    action="/management/user/delete/<%= user.user_id %>"
                    method="POST"
                    class="d-inline"
                    onsubmit="return confirm('Are you sure you want to delete this user?');"
                  >
                    <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>

    <!-- Bootstrap 5 JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    ></script>
  </body>
</html>
