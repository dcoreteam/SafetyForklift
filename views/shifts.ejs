<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Shift Report</title>
    <style>
      .filter-form {
        width: 80%;
        margin: 0 auto;
        margin-bottom: 20px;
      }
      .filter-form div {
        margin-bottom: 8px;
      }
      .export-buttons {
        text-align: center;
        margin: 10px 0;
      }
      .export-buttons a {
        display: inline-block;
        margin: 0 5px;
        padding: 8px 16px;
        background: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 4px;
      }
      table {
        border-collapse: collapse;
        width: 80%;
        margin: 0 auto;
      }
      th, td {
        border: 1px solid #ccc;
        padding: 8px;
        text-align: center;
      }
      th {
        background-color: #f2f2f2;
      }
      h1 {
        text-align: center;
      }
    </style>
  </head>
  <body>
    <h1>Shift Report</h1>

    <!-- ฟอร์มกรอง (method="GET" เพื่อส่ง query string) -->
    <form class="filter-form" method="GET" action="/shifts">
      <div>
        <label>Company: </label>
        <select name="company_id">
          <option value="">-- All Companies --</option>
          <% companies.forEach(company => { %>
            <option value="<%= company.id %>"
              <%= (filters.company_id == company.id) ? 'selected' : '' %>>
              <%= company.name %>
            </option>
          <% }) %>
        </select>
      </div>

      <div>
        <label>Card UID: </label>
        <input type="text" name="card_uid" value="<%= filters.card_uid || '' %>">
      </div>

      <div>
        <label>Staff Name: </label>
        <input type="text" name="staff_name" value="<%= filters.staff_name || '' %>">
      </div>

      <div>
        <label>Start Date: </label>
        <input type="date" name="start_date" value="<%= filters.start_date || '' %>">
      </div>

      <div>
        <label>End Date: </label>
        <input type="date" name="end_date" value="<%= filters.end_date || '' %>">
      </div>

      <div>
        <button type="submit">Filter</button>
      </div>
    </form>

    <!-- ปุ่ม Export CSV / Excel (ส่ง query string เดียวกัน) -->
    <div class="export-buttons">
      <a href="/shifts/export/csv?<%= Object.keys(filters).map(key => key + '=' + filters[key]).join('&') %>">
        Export CSV
      </a>
      <a href="/shifts/export/excel?<%= Object.keys(filters).map(key => key + '=' + filters[key]).join('&') %>">
        Export Excel
      </a>
    </div>

    <!-- ตารางแสดงผลลัพธ์ -->
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Staff Name</th>
          <th>Company</th>
          <th>Card UID</th>
          <th>Fleet Name</th>
          <th>Check In</th>
          <th>Check Out</th>
        </tr>
      </thead>
      <tbody>
        <% if (shifts.length === 0) { %>
          <tr>
            <td colspan="7">No records found.</td>
          </tr>
        <% } else { %>
          <% shifts.forEach(shift => { %>
            <tr>
              <td><%= shift.shift_log_id %></td>
              <td><%= shift.staff_name %></td>
              <td><%= shift.company_name %></td>
              <td><%= shift.card_uid %></td>
              <td><%= shift.fleet_name %></td>
              <td><%= shift.check_in %></td>
              <td><%= shift.check_out %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </body>
</html>
