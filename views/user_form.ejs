<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= formMode === 'add' ? 'Add User' : 'Edit User' %></title>
    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >
  </head>
  <body>
    <div class="container mt-4">
      <h1><%= formMode === 'add' ? 'Add User' : 'Edit User' %></h1>
      
      <!-- ถ้าเป็นโหมด add => action="/management/user/add"
           ถ้าเป็นโหมด edit => action="/management/user/edit/:id" -->
      <form
        method="POST"
        action="<%= formMode === 'add' ? '/management/user/add' : '/management/user/edit/' + user.id %>"
        class="row g-3"
      >
        <div class="col-md-6">
          <label for="username" class="form-label">Username:</label>
          <input
            type="text"
            name="username"
            id="username"
            class="form-control"
            value="<%= user ? user.username : '' %>"
            required
          >
        </div>

        <div class="col-md-6">
          <label for="password" class="form-label">Password:</label>
          <input
            type="text"
            name="password"
            id="password"
            class="form-control"
            value="<%= user ? user.password : '' %>"
            required
          >
        </div>

        <div class="col-md-6">
          <label for="role" class="form-label">Role:</label>
          <select name="role" id="role" class="form-select">
            <option value="super_admin" <%= (user && user.role === 'super_admin') ? 'selected' : '' %>>
              super_admin
            </option>
            <option value="customer_admin" <%= (user && user.role === 'customer_admin') ? 'selected' : '' %>>
              customer_admin
            </option>
          </select>
        </div>

        <div class="col-md-6">
          <label for="company_id" class="form-label">Company ID:</label>
          <input
            type="number"
            name="company_id"
            id="company_id"
            class="form-control"
            value="<%= user ? user.company_id : '' %>"
            required
          >
        </div>

        <div class="col-12">
          <button type="submit" class="btn btn-success">
            <%= formMode === 'add' ? 'Add User' : 'Save Changes' %>
          </button>
          <a href="/management/user" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>

    <!-- Bootstrap 5 JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    ></script>
  </body>
</html>
